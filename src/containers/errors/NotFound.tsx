import {Error} from "@components/errorPage/Error";

const NotFound = () => {
  return (
    <Error code={404} msg="صفحه مورد نظر در طلا و جواهر ریحان   موجود نیست." />
  );
};

export default NotFound;
